'use strict'

const ApiSpec = [

  /*{
    name: 'present-add',
    method: 'post',
    uri: '/present/add',
    enableJWT: true,
    schema: `
    joi.object().keys({
      gamename: joi.string().required(),
      icon: joi.string().required(),
      iconname: joi.string().required(),
      platformDisplay: joi.string().required(),
      startTime: joi.date().required(),
      expireTime: joi.date().required(),
      description: joi.string().required(),
      source: joi.binary().encoding('base64').required(),
    })
    `,
  },*/
  // todo add api spec

]

module.exports = ApiSpec
